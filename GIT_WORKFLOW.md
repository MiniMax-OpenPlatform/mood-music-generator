# Git 工作流程提醒 📝

## ⚠️ 重要提醒

**每次修改代码后，务必进行 Git 备份！**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 🔄 标准工作流程

### 1. 修改代码后

```bash
# 查看修改了哪些文件
git status

# 查看具体修改内容
git diff

# 或查看某个文件的修改
git diff main.py
```

### 2. 添加修改到暂存区

```bash
# 添加所有修改的文件
git add .

# 或只添加特定文件
git add main.py
git add templates/index.html
```

### 3. 提交修改

```bash
# 创建提交（包含清晰的说明）
git commit -m "描述你的修改内容"

# 更详细的提交信息
git commit -m "feat: 添加新功能

- 详细描述1
- 详细描述2"
```

### 4. 推送到 GitHub

```bash
# 推送到远程仓库
git push origin master

# 或简写
git push
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📋 提交信息规范

### 提交类型前缀：

- `feat:` 新功能
- `fix:` 修复 bug
- `docs:` 文档更新
- `style:` 代码格式调整（不影响功能）
- `refactor:` 重构代码
- `perf:` 性能优化
- `test:` 测试相关
- `chore:` 构建/工具相关

### 示例：

```bash
git commit -m "feat: 添加音乐下载进度条"
git commit -m "fix: 修复 API Key 验证错误"
git commit -m "docs: 更新 README 安装说明"
git commit -m "perf: 优化音乐生成速度"
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 🚀 快速备份命令

### 一键备份（推荐）

```bash
# 添加、提交、推送一气呵成
git add . && git commit -m "backup: $(date +%Y-%m-%d
